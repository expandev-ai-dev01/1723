import { Router } from 'express';

const router = Router();

/**
 * @summary External API routes configuration
 * @description Public endpoints accessible without authentication
 */

/**
 * @remarks External routes will be added here as features are implemented
 * @example router.use('/security', securityRoutes);
 * @example router.use('/public', publicRoutes);
 */

export default router;
